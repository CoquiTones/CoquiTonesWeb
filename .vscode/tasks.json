{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Generate mock data",
            "type": "shell",
            "command": "python ${workspaceFolder}/scripts/test/generate_mock_data.py",
            "problemMatcher": [
                "$python"
            ]
        }
    ]
}